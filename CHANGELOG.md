# Changelog

All notable changes to `yy` are recorded here, in the format of
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Versioning follows
[Semantic Versioning](https://semver.org/spec/v2.0.0.html); while the project is
pre-1.0, a breaking change is a minor bump.

Entries are not written from memory. They are derived from the pull request
titles since the last tag, which are Conventional Commits, so the derivation is
mechanical — see [`RELEASING.md`](RELEASING.md) step 3. Only `feat` and `fix`
are user-facing and appear here; everything else lives in the commit history.

## [Unreleased]

Nothing released yet. The repository is scaffolding until
[roadmap](docs/design/roadmap.md) Day 1 lands.
